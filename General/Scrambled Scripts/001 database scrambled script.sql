update [CO HRA COMPANY BANK DETAILS] set [Account Number] = 'XXXXXX'

delete from [per rem payroll calc]

delete from [per rem payroll calc audit]

delete from [per rem payrate audit]

delete from [per rem payrate approval]

delete from [PER REM Payroll Master Approval]

delete from [per rem payroll master audit]

delete from [CO SRC Bulk Engagement Upload]

delete from [PER REM Payroll Master Upload]

delete from [CO SRC Interface Table]

delete from [CO SRC Job Table]

delete from [PER LVE Leave Payroll Effect]

update [per hra personal]
set [reference] = [number]

delete from [per hra personal approval]

delete from [per hra personal audit]

update [per rem bank detail]
set [account number] = 'XXXXXXXXXX'

delete from [per rem bank detail audit]

delete from [per rem bank detail approval]

delete from [per rem GL Interface]

delete from [per rem gl interface history]